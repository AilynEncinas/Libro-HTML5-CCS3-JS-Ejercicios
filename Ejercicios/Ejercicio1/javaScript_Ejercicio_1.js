function cambiarColor() {
    document.body.style.backgroundColor="#0000FF";//Llamada al cuerpo del documento html para que cambie de color
}
document.addEventListener("click",cambiarColor);//accion para que cuando se haga un click se pueda cambiar el color